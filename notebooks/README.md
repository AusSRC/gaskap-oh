# Notebooks

To help users download data from the GASKAP-OH TAP interface

```
pip install requirements.txt
jupyter notebook
```

Then open `notebooks/access.ipynb`. A template notebook will support you through downloading data into a catalog and FITS files. You will be required to provide credentials to the web service (https://gaskap.aussrc.org/tap) in order to download product files.
